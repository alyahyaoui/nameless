/* eslint-disable no-console */
/* eslint-disable no-undef */
const hook = document.getElementById('root');
hook.innerText = 'hello world from main JS';

const form = document.createElement('form');
hook.appendChild(form);
